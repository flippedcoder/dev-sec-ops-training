version: 2.1

jobs:
    build-artifact:
      docker:
        - image: cimg/base:stable
      steps:
        - checkout
        - run:
            name: "building artifact..."
            command: "echo Artifact has been built!"

workflows:
  demo-flow:
    jobs:
      - build-artifact
